@model IEnumerable<admin_lovelypa_in.MPA_DETAILED_SYMPTOM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MPA_BASIC_SYMPTOM.symptom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.detailed_symptom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.priority)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MPA_BASIC_SYMPTOM.symptom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.detailed_symptom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.priority)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
